### LLM / Orchestrator:
- Claude Sonnet 4.6 (Primary) / GPT-4o (Fallback)

### Browser / Infrastructure:
- Playwright + Stagehand (AI Vision) on Browserbase Cloud

### CAPTCHA Solving:
- CapSolver (Primary) / 2Captcha (Fallback)

### Email Monitoring:
- Gmail API / imap_tools (OTP and magic link extraction)

### Database:
- PostgreSQL (Status, accounts, screenshots, logs)
